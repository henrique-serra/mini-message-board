<!DOCTYPE html>
<html lang="en">
<%- include('./partials/head.ejs', { title }) %>
<body>
    <%- include('./partials/nav.ejs') %>
    
    <div class="container">
        <h1>Message Board</h1>
        
        <% if (messages.length === 0) { %>
            <div class="message-card">
                <p>No messages yet. <a href="/new" class="btn">Create the first message!</a></p>
            </div>
        <% } else { %>
            <% messages.forEach(({ id, text, user, added }) => { %>
                <div class="message-card">
                    <div class="message-text"><%= text %></div>
                    <div class="message-meta">
                        <div>
                            <div class="message-author">By <%= user %></div>
                            <div class="message-date">Posted <%= added %></div>
                        </div>
                        <div class="message-actions">
                            <a href="/msgs/<%= id %>" class="btn">Read More</a>
                            <form action="/msgs/<%= id %>/delete" method="post" class="delete-form">
                                <button class="btn btn-danger" type="submit">Delete</button>
                            </form>
                        </div>
                    </div>
                </div>
            <% }) %>
        <% } %>
    </div>
    
    <%- include('./partials/footer.ejs') %>
</body>
</html>